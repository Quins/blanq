<% if (page !== undefined) { %>
	<% if (page.link) { %>
href="<%= page.link %>"
	<% } else { %>
href="<%= (environment == "production" ? "./" : "") %><%= page.template %><%= (environment == "production" ? ".html" : "") %>"
	<% } %>
<% } else { %>
href="#"
<% } %>